# TODO - GaGa Chat Deployment

## Plan
- [ ] Confirm Firebase Hosting config (firebase.json) uses dist as public dir, SPA rewrites, and CSP/cache headers
- [ ] Confirm deploy script builds then runs `firebase deploy --only hosting`
- [ ] Provide exact deploy command(s) to run in user terminal (Firebase CLI required)
- [ ] (Optional) Smoke-test: load /index.html, verify JS chunk loading, PWA assets, and SPA routing works

